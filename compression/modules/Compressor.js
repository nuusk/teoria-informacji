class Compressor {

  create(alphabet) {
    console.log(alphabet);

    let code;
    // tworzymy kod na podstawie którego będziemy kompresować dane

    return code;
  }

  encode(uncompressed) {
    let compressed;
    // kompresujemy tekst na podstawie kodu

    return compressed;
  }

  decode(compressed) {
    let uncompressed;

    return uncompressed;
  }

  save() {
    // zapisz kod oraz zakodowany tekst

  }

  load() {
    // wczytaj zakodowany tekst oraz kod

  }
}

module.exports = Compressor;