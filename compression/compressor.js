class Compressor {

  create(text) {
    let code;
    // tworzymy kod na podstawie którego będziemy kompresować dane

    return code;
  }

  encode(uncompressed) {
    let compressed;
    // kompresujemy tekst na podstawie kodu

    return compressed;
  }

  decode(compressed) {
    let uncompressed;

    return uncompressed;
  }

  save() {

  }

  load() {

  }

}